spring:
  thymeleaf:
    mode: LEGACYHTML5

feign:
  hystrix:
    enabled: true

eureka:
  client:
    serviceUrlDefaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost

server:
  port: 8806
