#spring.application.name = profesores-mysql-service
eureka:
  client:
    serviceUrlDefaultZone : http://localhost:8761/eureka/
  instance:
    hostname: localhost

server:
  port : 8801

proyectoset-oracle-service:
  ribbon:
    eureka:
      enabled: false
    listOfServers: localhost:8803
    ServerListRefreshInterval: 15000